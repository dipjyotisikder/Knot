/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "standalone", // for server rendering
    // experimental options can be added here if needed
    // Add more SEO/server rendering options as needed
};

module.exports = nextConfig;
